<!DOCTYPE html>
<html>
  <head>
    <title>CzytelniaProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <ul>
  <li><%= link_to "Strona główna", root_path %></li>
  <li><%= link_to "Biblioteki", libraries_path %></li>
  <% if logged_in? && current_user.admin %>
    <li><%= link_to "Dodaj uzytkownika", {:controller => "users", :action => "new", :library_id => current_user.library_id } %></li>
  <% end %>
  <% if logged_in? %>
    <li><%= link_to current_user.index, current_user %></li>
    <li><%= link_to "Wyloguj", logout_path %></li>
  <% else %>
    <li><%= link_to "Zaloguj", login_path %></li>
  <% end %>
</ul>
    <%= yield %>
  </body>
</html>
