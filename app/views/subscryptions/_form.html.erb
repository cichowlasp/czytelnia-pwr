<%= form_with(model: subscryption) do |form| %>
  <% if subscryption.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscryption.errors.count, "error") %> prohibited this subscryption from being saved:</h2>

      <ul>
        <% subscryption.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.label :level, style: "display: block" %>
    <%= form.text_field :level %>
  </div>

  <div>
    <%= form.label :library_id, style: "display: block" %>
    <%= form.text_field :library_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
